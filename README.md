# Mott MacDonald Toy Robot Game

How to run:

In the root directory run:

` $ npm install `

To build without running:

` $ npm build `

Start the game using:

` $ npm start `

Execute unit tests:

` $ npm test `
